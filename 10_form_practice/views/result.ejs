<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1><%= title %></h1>
    <ul>
      <li><%= userInfo.username%></li>
      <li><%= userInfo.gender%></li>
      <li>
        <%= userInfo.birthYear%>년 <%= userInfo.birthMonth%>월 <%=
        userInfo.birthDay%>일
      </li>
      <li><%= userInfo.hobby%></li>
      <% if(userInfo.color.result) { %>
      <li><%= userInfo.color.color %></li>
      <% }%> <% if(userInfo.number.result) { %>
      <li><%= userInfo.number.number %></li>
      <% }%>
    </ul>
  </body>
</html>
