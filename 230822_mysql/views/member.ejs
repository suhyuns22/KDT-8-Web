<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Document</title>
  </head>
  <body>
    <h1>회원 리스트 모두 조회</h1>
    <a href="/">홈으로 이동</a>
    <ul>
      <% for(let i = 0; i < lists.length; i++) { %>
      <li>
        <b><%= lists[i].userid %></b>
        <a href="/members/<%= lists[i].id %>"> <%= lists[i].name %> </a>
      </li>
      <% } %>
    </ul>
  </body>
</html>
